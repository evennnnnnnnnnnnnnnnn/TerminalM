# Setup Living Documents

Tag all documents in the mesh with lifecycle status.

# Actions

1. Scan all markdown files in `Mesh/`
2. Classify each as living or dead based on type
3. Add `#ld/living` or `#ld/dead` tag to frontmatter
