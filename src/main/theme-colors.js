/** Titlebar overlay colors per theme (Windows only). */
const TITLEBAR_COLORS = {
  dark:  { color: '#181825', symbolColor: '#cdd6f4' },
  light: { color: '#e6e9ef', symbolColor: '#4c4f69' },
};

/** Default window background color (Catppuccin Mocha base). */
const DEFAULT_BG = '#1e1e2e';

module.exports = { TITLEBAR_COLORS, DEFAULT_BG };
